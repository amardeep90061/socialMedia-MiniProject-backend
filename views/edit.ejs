<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <h5 class="mb-5">Edit post.</h5>
        <form method="post" action="/update/<%=post._id%>">
            <textarea placeholder="What's on your mind ?" class=" block p-3 w-1/3 resize-none outline-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""><%=post.content%></textarea>
            <input class="px-3 w-40 py-2 bg-yellow-500 text-sm block rounded-md mt-2" type="submit" value="Update post"> 
        </form>
    </div>
</body>
</html> 